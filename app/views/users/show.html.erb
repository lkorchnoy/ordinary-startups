<h3><%= @user.name %></h3>
<ul>
<% @user.startups.each do |startup| %>
<li><%= link_to user.startup.company, startup_path(startup) %></li>
<%#=startup.id%><%#= startup.company %>>
<% end %>
</ul>

<% if current_user %>
<%= link_to "delete account", @user, method: :delete %>
<% end %>
<%#= button_to "Logout", controller: "sessions", action: "destroy" %>